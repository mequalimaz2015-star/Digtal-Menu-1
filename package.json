{
  "name": "digital-menu-root",
  "version": "1.0.0",
  "scripts": {
    "build:all": "npm install --prefix frontend && npm run build --prefix frontend && npm install --prefix backend",
    "start": "node backend/server.js"
  }
}
